# Test Utils Module

## Purpose

The test-utils module hosts shared test doubles and helpers reused in unit tests.

## Usage

- Import these utilities in test files under `src/**/__tests__` to reduce
  repeated mock setup and improve test readability.
